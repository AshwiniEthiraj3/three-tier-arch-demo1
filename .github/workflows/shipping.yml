name: shipping CI

on:
  push:
    branches:
      - main
    paths:
      - 'shipping/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Echo service name
        run: echo "Running shipping pipeline"
